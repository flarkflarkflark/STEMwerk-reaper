STEMwerk installed.

Important:
Open REAPER and run STEMwerk.lua.

Use STEMwerk-SETUP.lua only as a support or repair path if something is missing after installation.

For 2.3.0.0, use the current installer set and verify downloads with the published SHA256 values before installing.
