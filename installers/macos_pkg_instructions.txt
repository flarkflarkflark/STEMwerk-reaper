# macOS packaging instructions (guide)

# 1. Create a signed .pkg using pkgbuild/productbuild
# Example (replace identifiers and paths):
# pkgbuild --root "dist" --identifier "com.yourcompany.stemwerk" --version "0.1" --install-location "/Applications/STEMwerk" "STEMwerk.pkg"
# productbuild --distribution distribution.xml --package-path ./packages "STEMwerk-installer.pkg"

# 2. Optionally sign with Developer ID and notarize with Apple
# See Apple docs for signing and notarization.

# The pkg should include the GUI executable and an installers folder that runs tools/gpu_installer.py
